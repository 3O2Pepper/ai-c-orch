import { describe, expect, it } from "vitest";

// Placeholder until Commit 2 adds real domain tests — proves the test
// runner and path alias are wired.
describe("test harness", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
